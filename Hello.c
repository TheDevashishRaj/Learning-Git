#include <stdio.h> // This is the preprocessor directive

int main() // The Main Function of the program. Program execution begins from here
{
    printf("Hello World\n"); // printf function is used to print a string onto the terminal window (screen).
    return 0; // return statement returns a code, 0 if successful, otherwise unsuccessful.
}
